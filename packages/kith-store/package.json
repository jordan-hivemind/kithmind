{
  "name": "@repo/kith-store",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.10.0"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "node": "./dist/index.js",
      "default": "./src/index.ts"
    },
    "./identity": {
      "types": "./src/identity/index.ts",
      "node": "./dist/identity/index.js",
      "default": "./src/identity/index.ts"
    }
  },
  "scripts": {
    "build": "pnpm --filter @repo/finance-contract build && pnpm --filter @repo/pg build && pnpm --filter @repo/worker-protocol build && tsc -p tsconfig.json",
    "check-types": "pnpm --filter @repo/finance-contract check-types && pnpm --filter @repo/pg check-types && pnpm --filter @repo/worker-protocol check-types && tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && node --test test/*.test.mjs",
    "test:once": "pnpm run build && node --test test/*.test.mjs",
    "test:integration": "pnpm run build && node --test integration/*.test.mjs"
  },
  "dependencies": {
    "@repo/finance-contract": "workspace:*",
    "@repo/pg": "workspace:*",
    "@repo/worker-protocol": "workspace:*",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@repo/finance-archive": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "25.3.3",
    "@types/pg": "^8.15.5",
    "typescript": "^5.7.0"
  }
}
