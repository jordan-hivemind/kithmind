{
  "name": "@repo/adapter-morgan-stanley",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.10.0"
  },
  "exports": {
    ".": "./src/adapter.mjs",
    "./session": "./src/bridge.mjs"
  },
  "scripts": {
    "build": "echo 'no build step: plain ES modules, nothing to compile' && exit 0",
    "check-types": "echo 'no check-types step: plain JavaScript, no type checker configured' && exit 0",
    "test": "node --test test/*.test.mjs",
    "test:once": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@repo/finance-archive": "workspace:*",
    "pdfjs-dist": "5.4.530"
  }
}
