{
  "name": "@repo/kith-migrate",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.10.0"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "node": "./dist/index.js",
      "default": "./src/index.ts"
    }
  },
  "bin": {
    "kith-migrate": "./dist/cli.js"
  },
  "scripts": {
    "build": "pnpm --filter @repo/kith-store build && tsc -p tsconfig.json",
    "check-types": "pnpm --filter @repo/kith-store check-types && tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && node --test test/*.test.mjs",
    "test:once": "pnpm run build && node --test test/*.test.mjs",
    "test:integration": "pnpm run build && node --test integration/*.test.mjs",
    "generate:migration": "pnpm run build && node dist/cli.js ddl:generate --out ../kith-store/migrations/004_kith_migrate_tables.sql"
  },
  "dependencies": {
    "@repo/kith-store": "workspace:*",
    "@repo/pg": "workspace:*",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/node": "25.3.3",
    "@types/pg": "^8.15.5",
    "typescript": "^5.7.0"
  }
}
